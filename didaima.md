# 收款码模板编辑器最终方案

## 一、项目目标

实现一个 **React + TypeScript** 的收款码模板编辑器，用于配置并生成模板图片。

该编辑器支持：

- 自定义画布宽高
- 上传并裁剪背景图
- 拖拽、缩放二维码
- 配置编码文字样式
- 导出高清图片并上传
- 支持接口回显编辑

---

## 二、技术栈要求

- React
- TypeScript
- react-konva
- Ant Design
- react-easy-crop 或 cropperjs

---

## 三、核心业务规则

### 1. 画布

- 画布宽度、高度支持用户输入
- 输入框只允许输入数字
- 单位固定显示为 `px`
- 用户不能输入 `px` 字符串
- 修改宽高后，画布实时更新
- 画布宽度最大 `500px`
- 画布高度最大 `600px`
- 导出尺寸也受上述上限约束，避免超大画布导致浏览器内存占用过高

### 2. 背景图

- 不需要背景颜色
- 只允许一张背景图
- 第二次上传直接覆盖第一次
- 上传后必须进入裁剪流程
- 裁剪完成后直接上传裁剪结果图
- 背景图显示时必须保持等比例
- 不允许背景图被拉伸变形
- 支持删除背景图
- 不需要保留原图用于二次编辑

### 3. 二维码

- 支持拖拽
- 支持缩放
- 只允许等比例缩放
- 不允许非等比例拉伸

### 4. 编码文字

- 编码文字不是自由拖拽文本
- 只能上下移动，不能左右自由拖拽
- 水平方向始终与二维码对齐
- 默认显示在二维码下方
- 支持修改：
  - 字号
  - 颜色
- 是否显示编码由业务开关控制

### 5. 导出

- 最终不是只保存 JSON
- 需要前端直接导出高清图片
- 导出后上传，拿到 `imageUrl`
- 新增/编辑接口会返回编辑参数，用于页面回显和再次编辑

---

## 四、页面结构

采用三栏布局：

### 左侧：基础信息 + 画布设置

包含：

- 所属品牌
- 模板名称
- 状态（启用/禁用）
- 是否默认
- 是否显示编码
- 画布宽度
- 画布高度
- 上传背景图
- 重新上传背景图
- 删除背景图
- 导出按钮
- 保存按钮

### 中间：可编辑画布

包含：

- 背景图
- 二维码
- 编码文字

支持：

- 选中二维码
- 拖拽二维码
- 缩放二维码
- 选中编码文字后仅沿 Y 轴拖动

### 右侧：属性面板

根据当前选中元素显示不同属性：

#### 选中二维码时

- x
- y
- width
- height
- 必须等比例拉伸

#### 选中编码文字时

- offsetY 或 y
- fontSize
- color

---

## 五、前端内部状态设计

前端编辑器内部维护一份页面级状态，用于驱动画布渲染、属性联动和导出。

注意：

- 当前不需要引入 `Zustand`
- 组件内使用 `useState` / `useReducer` 即可
- 页面状态既服务当前编辑流程，也用于接口回显时恢复编辑器内容

### 推荐前端内部状态结构

```ts
interface EditorStateModel {
  canvas: {
    width: number;
    height: number;
    backgroundImage: {
      url: string;
      fit: "cover";
    } | null;
  };
  qrcode: {
    x: number;
    y: number;
    width: number;
    height: number;
    visible: boolean;
  };
  codeText: {
    text: string;
    offsetY: number;
    fontSize: number;
    color: string;
    visible: boolean;
  };
  showCodeText: boolean;
}
```
